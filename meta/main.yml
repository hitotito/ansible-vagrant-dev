---
galaxy_info:
  role_name: vagrant_dev
  author: Hitoshi Okonogi
  description: role to install vagrant development dependencies
  company: hitotito
  license: MIT
  min_ansible_version: 2.0
  platforms:
    - name: Debian
      versions:
        - jessie
        - stretch
  galaxy_tags:
    - development
    - vagrant

dependencies:
  - role: hitotito.custom-facts
    custom_facts_name: hitotito_vagrant_dev
    custom_facts_file: vagrant_dev.fact
